$mod = SUPER
input{
	kb_layout=de
}


monitor=,preferred,auto,1

bind = $mod, RETURN, exec, kitty

bind = $mod, SPACE, exec, rofi -show drun

bind = $mod, Q, killactive,

bind = $mod SHIFT, E, exit,

bind = $mod, P, exec, hyprlock

bind = , Print, exec, grim -g "$(slurp)" - | tee "/home/lenz/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png" | wl-copy

bind = SHIFT, Print, exec, grim - | tee "/home/lenz/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png" | wl-copy

bind = $mod, S, fullscreen,

bind = $mod SHIFT, W, exec, pkill waybar && waybar &

bind = $mod, F, exec, firefox



bind = $mod, H, movefocus, l
bind = $mod, L, movefocus, r
bind = $mod, K, movefocus, u
bind = $mod, J, movefocus, d

bind = $mod SHIFT, H, movewindow, l
bind = $mod SHIFT, L, movewindow, r
bind = $mod SHIFT, K, movewindow, u
bind = $mod SHIFT, J, movewindow, d


bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9


bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9



# Sound
bind = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bind = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bind = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

# Brightness
bind = , XF86MonBrightnessUp, exec, brightnessctl set +10%
bind = , XF86MonBrightnessDown, exec, brightnessctl set 10%-



exec-once = ~/.local/bin/waybar-restart.sh &
exec-once = nm-apple
exec-once = poolkit-kde-authentication-agent-1
exec-once = swww-daemon
exec-once = swww img /home/lenz/Pictures/Wallpapers/wp3.png


general {
    border_size = 3

    col.active_border = rgba(ff9ad5ff) rgba(8fd3f4ff) 45deg
    col.inactive_border = rgba(553355aa)
}


windowrule = match:class kitty, opacity 0.78 0.78


layerrule = blur on, ignore_alpha 0, match:namespace rofi


decoration {
    rounding = 16

    blur {
        enabled = true
        size = 14
        passes = 3
        noise = 0.02
        contrast = 1.1
        brightness = 1.0
        vibrancy = 0.2
    }
}


misc {
    enable_swallow = false
}
